# Copia a .env en local. En Render, define estas variables en el panel (Environment).

PORT=3000

# Cadena de conexión Atlas (usa una de las dos; el código acepta ambas)
MONGODB_URI=mongodb+srv://USUARIO:PASSWORD@cluster.mongodb.net/nombre_bd?retryWrites=true&w=majority
# MONGO_URI=mongodb+srv://...

JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=1h
